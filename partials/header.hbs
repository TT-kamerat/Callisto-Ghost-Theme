<header class="header" role="banner">
    <h1 class="header__title">
        <a title="{{@site.title}}" href='{{@site.url}}'>{{{@site.title}}}</a>
    </h1>
    <nav class="header__nav" role="navigation">
        {{#foreach navigation}}
            <a class="header__link" href="{{url absolute="true"}}">{{label}}</a>
        {{/foreach}}
    </nav>
</header>